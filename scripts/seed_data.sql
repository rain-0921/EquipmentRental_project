-- =====================================================================
-- seed_data.sql : optional extra demo data (idempotent inserts)
-- =====================================================================
USE smart_rental;

-- Nothing extra right now; the schema script already seeds enough
-- sample users and equipment to exercise every screen in the GUI.
SELECT 'Seed data already loaded by setup_database.sql' AS info;